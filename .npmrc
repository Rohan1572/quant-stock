# npm configuration for quant-stock monorepo
# 
# Using legacy-peer-deps to allow esbuild-plugin-pino@2.3.3 to work with
# esbuild@0.28.2. Future: update esbuild-plugin-pino to a version compatible
# with esbuild@0.28+

legacy-peer-deps=true
